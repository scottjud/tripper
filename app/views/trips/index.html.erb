<div class="page-header">
  <h1>Listing trips</h1>
</div>

  

<% @trips.each do |trip| %>

  <%= trip.name %>
  <%= trip.duration %>
  <%= link_to 'See Recommendations' %>
  <%= link_to 'Show', trip %> 
  <%= link_to 'Edit', edit_trip_path(trip) %>
  <%= link_to 'Destroy', trip, method: :delete, data: { confirm: 'Are you sure?' } %></p>

</div>  

<% end %>


<br>

<%= link_to 'New Trip', new_trip_path, :class=>"btn btn-primary btn-lg" %>
 